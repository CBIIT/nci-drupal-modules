<?php

/**
 * Implements hook_install().
 */
function wbm_default_state_install() {
  // Workbench moderation has weight 5 and we need to beat that!
  db_update('system')
    ->fields(array('weight' => 10))
    ->condition('name', 'wbm_default_state')
    ->execute();
}
